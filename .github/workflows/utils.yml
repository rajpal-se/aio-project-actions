name: Utils Workflow

on:
  workflow_dispatch:

jobs:
  types:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run Utils Tasks
        run: |
          echo "Running utils tasks..."
          echo 90
